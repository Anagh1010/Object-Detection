# Optional dependencies for running the exploratory training notebooks.
-r requirements.txt
jupyterlab
pandas>=1.5.0
matplotlib>=3.6.0
seaborn>=0.12.0
opencv-python>=4.7.0
tqdm>=4.64.0
scipy>=1.10.0
scikit-posthocs>=0.7.0

# The notebook's optional ReliefF analysis uses `skrebate`, which does not
# publish a compatible release for Python 3.12. Use Python 3.10 or earlier
# if that analysis is required, then install skrebate separately.
